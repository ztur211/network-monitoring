using Avalonia.Controls;

namespace NodeScope.Desktop.Views;

/// <summary>The shell window. Content is a placeholder until auth + shell land.</summary>
internal sealed partial class MainWindow : Window
{
    public MainWindow()
    {
        InitializeComponent();
    }
}
