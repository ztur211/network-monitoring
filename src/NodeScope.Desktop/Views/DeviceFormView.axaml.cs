using Avalonia.Controls;

namespace NodeScope.Desktop.Views;

/// <summary>The device create/edit modal; all behavior lives in the view model.</summary>
internal sealed partial class DeviceFormView : UserControl
{
    public DeviceFormView()
    {
        InitializeComponent();
    }
}
