using Avalonia.Controls;

namespace NodeScope.Desktop.Views;

/// <summary>The authenticated account gate shown before organization-scoped features.</summary>
internal sealed partial class OrganizationAccessView : UserControl
{
    public OrganizationAccessView()
    {
        InitializeComponent();
    }
}
