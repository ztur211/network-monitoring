{
  "//": "Shared TypeScript settings for the pure library packages (shared, client, probe), which had byte-identical tsconfigs. Each package keeps only its own paths (outDir/rootDir/include) — those must stay per-package because `extends` resolves relative paths against the file that declares them. api (decorators/commonjs), agent (NodeNext), and desktop (project references) intentionally diverge and do NOT extend this base.",
  "compilerOptions": {
    "strict": true,
    "target": "ES2021",
    "module": "commonjs",
    "declaration": true,
    "declarationMap": true,
    "sourceMap": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true
  }
}
